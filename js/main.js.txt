// Interactive alert handlers
const showAlert = (msg) => {
  alert(msg);
};

// Button event listeners
document.getElementById('demoAlertBtn')?.addEventListener('click', () => {
  showAlert('✨ OmniAI Nexus Live Demo: Experience AI-powered campaign automation, smart content generation and social analytics. (Full interactive prototype available on request)');
});

document.getElementById('getStartedBtn')?.addEventListener('click', () => {
  showAlert('🚀 Welcome to OmniAI Nexus! Secure account creation with email/Google/Apple + MFA — Start your 14-day free trial. No credit card required.');
});

document.getElementById('heroCtaBtn')?.addEventListener('click', () => {
  showAlert('🌟 Launching OmniAI Nexus: Unified dashboard connecting AI engines & social platforms. Get early access now!');
});

document.getElementById('finalCtaBtn')?.addEventListener('click', () => {
  showAlert('📅 Book a personalized demo: See how OmniAI Nexus automates multi-channel marketing, predictive analytics and AI video generation for your brand.');
});

document.getElementById('marketplaceNotify')?.addEventListener('click', () => {
  showAlert('🗂️ Join waitlist for OmniAI Marketplace — be first to access premium AI plugins, templates & automation flows.');
});

// Add smooth scroll for anchor links
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function(e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    if (target) {
      target.scrollIntoView({ behavior: 'smooth' });
    }
  });
});

console.log('OmniAI Nexus — Intelligent Marketing Platform Ready');